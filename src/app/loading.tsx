export default function Loading() {
  return (
    <main className="relative flex min-h-[100svh] items-center justify-center">
      <span
        aria-label="Loading"
        className="font-mono text-2xl tracking-[0.2em] text-muted-foreground animate-cashu-loading"
      >
        […]
      </span>
    </main>
  );
}
