export default function Loading() {
  return (
    <main className="relative flex min-h-[100svh] items-center justify-center">
      <span
        aria-label="Loading"
        className="font-display text-2xl font-extrabold lowercase text-primary animate-zeile-loading"
      >
        zeile
      </span>
    </main>
  );
}
